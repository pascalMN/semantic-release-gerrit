export interface Context {
  commit: string;
  currentTag: string;
  date: string;
  host: string;
  issue: string;
  linkCompare: string;
  linkReference: string;
  owner: string;
  previousTag: string;
  repository: string;
  version: string;
  repoUrl: string;
}
