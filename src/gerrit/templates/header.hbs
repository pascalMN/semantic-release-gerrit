{{#if isPatch~}}
  ##
{{~else~}}
  #
{{~/if}} {{#if @root.gerrit~}}
  [{{version}}]({{~@root.gerrit}}/plugins/gitiles/{{~@root.projectName}}/+/refs/tags/{{currentTag}})
{{~else}}
  {{~version}}
{{~/if}}
{{~#if title}} "{{title}}"
{{~/if}}
{{~#if date}} ({{date}})
{{/if}}
