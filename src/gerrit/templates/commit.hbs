*{{#if scope}} **{{scope}}:**
{{~/if}} {{#if subject}}
  {{~subject}}
{{~else}}
  {{~header}}
{{~/if}}

{{~#if review}}
  {{#if @root.gerrit}}
    ([{{~hash}}]({{@root.gerrit}}/{{~@root.commit}}/{{review.changeNumber}}/{{review.patchSet}}))
  {{~else}}
    ({{~hash}})
  {{/if}}
{{~else}}
  ({{~hash}})
{{~/if}}
{{#each references}}
  {{~#if @root.issues}}
    {{action}} [{{prefix}}{{issue}}]({{@root.issues}}/{{issue}})
  {{else}}
    {{raw}}
  {{~/if}}
{{/each}}

