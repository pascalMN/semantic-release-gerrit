*{{#if scope}} **{{scope}}:**
{{~/if}} {{#if subject}}
  {{~subject}}
{{~else}}
  {{~header}}
{{~/if}}

{{~#if review}}
  {{#if @root.gerrit}}
    ([{{~hash}}]({{@root.gerrit}}/{{~@root.commit}}/{{review.changeNumber}}/{{review.patchSet}}))
  {{~else}}
    ({{~hash}})
  {{/if}}
{{~else}}
  ({{~hash}})
{{~/if}}

