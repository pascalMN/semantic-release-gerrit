{
  "name": "semantic-release-gerrit",
  "private": false,
  "license": "MIT",
  "main": "public_api.js",
  "scripts": {
    "build": "tsc --build tsconfig.json && npm run copy:package.json",
    "clean": "rimraf dist",
    "copy:package.json": "copy-pkg package.json dist/package.json --only name,version,license,main"
  },
  "devDependencies": {
    "copy-pkg": "^1.0.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.0.1"
  }
}
